# 🚀 Сборка APK через GitHub Actions

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Название: `planner-app` (или любое другое)
4. Выберите **"Public"** или **"Private"**
5. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите **"Create repository"**

---

## Шаг 2: Загрузите код на GitHub

Откройте PowerShell в папке проекта (`C:\book`) и выполните:

```powershell
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/planner-app.git

# Загрузите код
git branch -M main
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Используйте Personal Access Token вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Права: `repo` (полный доступ к репозиториям)

---

## Шаг 3: Запустите сборку APK

1. Откройте ваш репозиторий на GitHub
2. Перейдите во вкладку **"Actions"** (сверху)
3. В левом меню выберите **"Build Android APK"**
4. Нажмите **"Run workflow"** (справа)
5. Выберите ветку `main`
6. Нажмите зеленую кнопку **"Run workflow"**

---

## Шаг 4: Дождитесь сборки

- Сборка займет **2-5 минут**
- Вы увидите прогресс в реальном времени
- Когда сборка завершится, появится зеленая галочка ✅

---

## Шаг 5: Скачайте APK

1. Откройте завершенный workflow (зеленая галочка)
2. Прокрутите вниз до раздела **"Artifacts"**
3. Нажмите на **"app-debug.apk"**
4. APK скачается на ваш компьютер

---

## Шаг 6: Установите APK на телефон

### Вариант A: Через USB
1. Подключите телефон к компьютеру
2. Перекиньте APK на телефон
3. Откройте APK на телефоне
4. Разрешите установку из неизвестных источников

### Вариант B: Через облако/email
1. Загрузите APK в Google Drive / Dropbox / Яндекс.Диск
2. Откройте на телефоне
3. Скачайте и установите

### Вариант C: Через ADB (если установлен)
```bash
adb install app-debug.apk
```

---

## ✅ Готово!

Теперь у вас есть APK на телефоне. Не забудьте:
1. Запустить сервер на компьютере: `start-server.bat`
2. Настроить IP в приложении: Меню → ⚙️ Настройки сервера

---

## ❓ Проблемы?

**"Actions не работает" / "Не вижу вкладку Actions":**
- Убедитесь, что репозиторий не пустой
- Проверьте, что код загружен на GitHub

**"Workflow не запускается":**
- Убедитесь, что файл `.github/workflows/build-apk.yml` существует
- Проверьте, что вы нажали "Run workflow" → "Run workflow"

**"Сборка падает с ошибкой":**
- Откройте workflow и посмотрите логи
- Убедитесь, что все файлы загружены (особенно `package.json`, `capacitor.config.json`)

**"Не могу скачать APK":**
- Убедитесь, что сборка завершилась успешно (зеленая галочка)
- Artifacts доступны 90 дней после сборки


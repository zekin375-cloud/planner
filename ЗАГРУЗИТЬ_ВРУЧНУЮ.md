# 📤 Загрузка кода на GitHub (если git push не работает)

## Вариант 1: Через веб-интерфейс GitHub

1. Откройте https://github.com/zekin375-cloud/planner-app
2. Если репозиторий пустой, GitHub покажет инструкции
3. Выберите **"uploading an existing file"**
4. Перетащите все файлы из папки `C:\book` (кроме `.git`, `node_modules`, `__pycache__`)
5. Нажмите **"Commit changes"**

---

## Вариант 2: Использовать GitHub Desktop

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Откройте GitHub Desktop
3. File → Add Local Repository → выберите `C:\book`
4. Нажмите **"Publish repository"**
5. Выберите `zekin375-cloud/planner-app`

---

## Вариант 3: Исправить авторизацию Git

### Если репозиторий приватный, нужен Personal Access Token:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Права: `repo` (полный доступ)
4. Скопируйте токен

### Затем используйте токен вместо пароля:

```powershell
git push -u origin main
```

При запросе:
- Username: `zekin375-cloud`
- Password: **вставьте токен** (не пароль!)

---

## Вариант 4: Проверить URL репозитория

Убедитесь, что URL правильный:
- Откройте https://github.com/zekin375-cloud/planner-app
- Если репозиторий существует, скопируйте URL со страницы
- Выполните:
  ```powershell
  git remote set-url origin [СКОПИРОВАННЫЙ_URL]
  git push -u origin main
  ```

---

## После успешной загрузки:

1. Откройте https://github.com/zekin375-cloud/planner-app
2. Перейдите в **Actions**
3. **Build Android APK** → **Run workflow** → **Run workflow**
4. Дождитесь сборки (2-5 минут)
5. Скачайте APK из **Artifacts**


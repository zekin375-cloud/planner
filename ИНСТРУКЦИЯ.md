# Инструкция: Компьютер как сервер, телефон как клиент

## 🚀 Быстрый старт

### Шаг 1: На компьютере

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите сервер:**
   ```bash
   start-server.bat    # Windows
   ```
   
   Скрипт покажет IP адрес, например: `http://192.168.1.100:5000`

### Шаг 2: Проверка подключения (на телефоне)

1. **Откройте браузер на телефоне**
2. **Введите адрес:** `http://ВАШ_IP:5000/check`
   (замените ВАШ_IP на адрес из шага 1, например: `http://192.168.1.100:5000/check`)
3. **Нажмите "Проверить подключение"**
4. Если видите ✅ "Подключение успешно" - все работает!

### Шаг 3: Настройка приложения на телефоне

1. **Установите APK** (соберите через GitHub Actions или вручную)

2. **Откройте приложение** → Меню (👤) → **"⚙️ Настройки сервера"**

3. **Введите IP адрес** из шага 1, например: `http://192.168.1.100:5000`

4. **Готово!** Приложение подключится к серверу

## ⚠️ Важно!

✅ **Компьютер и телефон в одной Wi-Fi сети** - это уже настроено!

- Сервер должен быть **запущен на компьютере**
- Файрвол должен разрешать подключения на порт **5000**
- При первом запуске Windows может спросить разрешение для файрвола - разрешите

## 🔧 Решение проблем

**Не подключается:**
1. Проверьте IP: `python get-local-ip.py`
2. Проверьте файрвол (разрешите Python)
3. Убедитесь, что в одной сети

**Проверка подключения:**
1. Откройте в браузере телефона: `http://ВАШ_IP:5000/check`
2. Или просто: `http://ВАШ_IP:5000` (должна открыться страница приложения)
3. Страница проверки покажет статус подключения и правильный URL

## 📁 Где хранятся данные?

Все данные в файле `planner.db` на **компьютере**.

Телефон только отправляет запросы и получает данные.


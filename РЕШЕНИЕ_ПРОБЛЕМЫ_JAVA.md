# ⚠️ Проблема: Требуется Java 11

Android Gradle Plugin требует **Java 11 или выше**, а у вас установлена **Java 8**.

## ✅ Решение 1: Обновить Java (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Скачайте Java 11 или выше
- **Java 11 (LTS):** https://adoptium.net/temurin/releases/?version=11
- **Java 17 (LTS, рекомендуется):** https://adoptium.net/temurin/releases/?version=17
- **Java 21 (последняя LTS):** https://adoptium.net/temurin/releases/?version=21

### Шаг 2: Установите Java
1. Скачайте установщик для Windows
2. Запустите и установите
3. **Важно:** Запомните путь установки (например: `C:\Program Files\Eclipse Adoptium\jdk-17.0.9+9`)

### Шаг 3: Настройте переменные окружения

**Вариант A: Через PowerShell (временно, только для текущей сессии):**
```powershell
$env:JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-17.0.9+9"
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"
```

**Вариант B: Постоянно (через системные настройки):**
1. Нажмите `Win + R`, введите `sysdm.cpl`, нажмите Enter
2. Вкладка **"Дополнительно"** → **"Переменные среды"**
3. В **"Системные переменные"**:
   - Создайте/измените `JAVA_HOME` → укажите путь к Java (например: `C:\Program Files\Eclipse Adoptium\jdk-17.0.9+9`)
   - В `Path` добавьте: `%JAVA_HOME%\bin`
4. Перезапустите PowerShell/CMD

### Шаг 4: Проверьте версию
```powershell
java -version
```
Должно показать версию 11 или выше.

### Шаг 5: Соберите APK
```powershell
cd android
.\gradlew assembleDebug
```

---

## ✅ Решение 2: Использовать GitHub Actions (БЕЗ УСТАНОВКИ JAVA)

Это самый простой способ - не нужно устанавливать Java!

### Шаг 1: Загрузите код на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/planner-app.git
git push -u origin main
```

### Шаг 2: Соберите APK на GitHub
1. Откройте репозиторий на GitHub
2. Вкладка **"Actions"**
3. **"Build Android APK"** → **"Run workflow"**
4. Дождитесь завершения (2-5 минут)

### Шаг 3: Скачайте APK
- Откройте завершенный workflow
- Внизу в **"Artifacts"** нажмите **"app-debug.apk"**

---

## ❓ Какой способ выбрать?

- **GitHub Actions** - если не хотите устанавливать Java (рекомендуется!)
- **Обновить Java** - если хотите собирать APK локально

---

## 📝 После установки Java

Если вы обновили Java, верните настройки Gradle к исходным:

```gradle
// android/build.gradle
classpath 'com.android.tools.build:gradle:8.0.0'

// android/gradle/wrapper/gradle-wrapper.properties
distributionUrl=https\://services.gradle.org/distributions/gradle-8.0.2-all.zip
```



# 🔑 Использование Personal Access Token

## После создания токена:

### Вариант 1: Ввести токен при push

```powershell
git push -u origin main
```

Когда Git попросит:
- **Username:** `zekin375-cloud`
- **Password:** **вставьте токен** (не пароль!)

---

### Вариант 2: Сохранить токен в Git Credential Manager

```powershell
# Git автоматически спросит токен при первом push
git push -u origin main
```

Введите:
- Username: `zekin375-cloud`
- Password: `ваш_токен`

Git сохранит токен для будущих операций.

---

### Вариант 3: Использовать токен в URL (временно)

```powershell
git remote set-url origin https://ваш_токен@github.com/zekin375-cloud/planner.git
git push -u origin main
```

**⚠️ Внимание:** После успешной загрузки лучше вернуть обычный URL:
```powershell
git remote set-url origin https://github.com/zekin375-cloud/planner.git
```

---

## ✅ После успешной загрузки:

1. Откройте https://github.com/zekin375-cloud/planner
2. Перейдите в **Actions**
3. **Build Android APK** → **Run workflow** → **Run workflow**
4. Дождитесь сборки (2-5 минут)
5. Скачайте APK из **Artifacts**


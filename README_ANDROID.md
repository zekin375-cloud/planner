# Сборка Android APK

## 🚀 Самый простой способ (БЕЗ Android Studio)

### Через GitHub Actions (РЕКОМЕНДУЕТСЯ)

1. Загрузите код на GitHub
2. Откройте Actions → Build Android APK → Run workflow
3. Скачайте готовый APK из Artifacts
4. Установите на телефон

**Подробнее:** см. `BUILD_APK_SIMPLE.md`

---

## Быстрый старт (с Android SDK)

### 1. Установите зависимости

```bash
npm install
```

### 2. Соберите проект

```bash
npm run build
```

### 3. Добавьте Android платформу (первый раз)

```bash
npm run cap:add:android
```

### 4. Синхронизируйте с Android

```bash
npm run cap:sync
```

### 5. Откройте в Android Studio

```bash
npm run cap:open:android
```

## Важные замечания

⚠️ **Flask на Android**: Для запуска Flask приложения на Android устройстве потребуется дополнительная настройка:

1. **Использовать Chaquopy** для запуска Python на Android
2. Или **переписать на чистом JavaScript** с использованием IndexedDB вместо SQLite
3. Или **использовать PWA** и запускать Flask на сервере

## Рекомендуемый подход

Для упрощения, рекомендуется:

1. Создать версию приложения на чистом JavaScript
2. Использовать IndexedDB для хранения данных
3. Упаковать в APK через Capacitor

Это избавит от необходимости запускать Python/Flask на Android устройстве.

## Структура проекта

```
book/
├── android/              # Android проект (создается автоматически)
├── dist/                 # Собранные файлы
├── static/               # Статические файлы
├── templates/            # HTML шаблоны
├── app.py                # Flask приложение
├── database.py           # Работа с БД
├── capacitor.config.json # Конфигурация Capacitor
└── package.json          # Node.js зависимости
```


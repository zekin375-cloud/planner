# 📱 Как собрать APK - Простая инструкция

## ⭐ Вариант 1: Через GitHub (САМЫЙ ПРОСТОЙ - без установки Android SDK)

### Шаг 1: Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/planner-app.git
git push -u origin main
```

### Шаг 2: Соберите APK

1. Откройте репозиторий на GitHub
2. Перейдите в **"Actions"** (вкладка сверху)
3. Выберите **"Build Android APK"**
4. Нажмите **"Run workflow"** → **"Run workflow"**
5. Дождитесь завершения (2-5 минут)

### Шаг 3: Скачайте APK

1. Откройте завершенный workflow
2. Внизу в разделе **"Artifacts"** нажмите **"app-debug.apk"**
3. APK скачается на компьютер

### Шаг 4: Установите на телефон

1. Перекиньте APK на телефон
2. Откройте APK
3. Разрешите установку (если спросит)
4. Готово! ✅

---

## 🔧 Вариант 2: Локально (если есть Android SDK)

### Windows (CMD):

```cmd
build-apk.bat
```

### Windows (PowerShell):

```powershell
.\build-apk-ps.ps1
```

Или вручную в папке android:
```powershell
cd android
.\gradlew assembleDebug
```

### Или вручную:

```bash
# 1. Установите зависимости
npm install

# 2. Соберите проект
npm run build

# 3. Добавьте Android (первый раз)
npm run cap:add:android

# 4. Синхронизируйте
npm run cap:sync

# 5. Соберите APK
cd android
gradlew assembleDebug
```

APK будет в: `android\app\build\outputs\apk\debug\app-debug.apk`

---

## 📋 После установки APK

1. **Запустите сервер на компьютере:**
   ```bash
   start-server.bat
   ```
   Запомните IP (например: `http://192.168.1.100:5000`)

2. **Откройте приложение на телефоне**

3. **Настройте сервер:**
   - Меню (👤) → "⚙️ Настройки сервера"
   - Введите IP: `http://192.168.1.100:5000`

4. **Готово!** Приложение подключится к серверу

---

## ❓ Проблемы?

**Не могу собрать через GitHub:**
- Убедитесь, что код загружен
- Проверьте файл `.github/workflows/build-apk.yml`

**Хочу собрать локально:**
- Нужен Android SDK (см. `BUILD_APK_SIMPLE.md`)
- Или используйте GitHub Actions (проще!)

**APK не устанавливается:**
- Включите "Установка из неизвестных источников"
- Или: `adb install app-debug.apk`


# Как собрать APK - Простая инструкция

## 🚀 Вариант 1: Через GitHub Actions (САМЫЙ ПРОСТОЙ) ⭐

### Шаг 1: Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/planner-app.git
git push -u origin main
```

### Шаг 2: Запустите сборку

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **"Actions"** (вкладка сверху)
3. Выберите **"Build Android APK"**
4. Нажмите **"Run workflow"** → **"Run workflow"**

### Шаг 3: Скачайте APK

1. Дождитесь завершения сборки (2-5 минут)
2. Откройте завершенный workflow
3. Внизу страницы в разделе **"Artifacts"** нажмите **"app-debug.apk"**
4. APK скачается на компьютер

### Шаг 4: Установите на телефон

1. Перекиньте APK на телефон (через USB, email, облако)
2. Откройте APK на телефоне
3. Разрешите установку из неизвестных источников (если спросит)
4. Установите

**Готово!** ✅

---

## 🔧 Вариант 2: Локально (если установлен Android SDK)

### Требования:
- Node.js (https://nodejs.org/)
- Java JDK 11+
- Android SDK

### Шаги:

```bash
# 1. Установите зависимости
npm install

# 2. Соберите проект
npm run build

# 3. Добавьте Android платформу (первый раз)
npm run cap:add:android

# 4. Синхронизируйте
npm run cap:sync

# 5. Соберите APK
cd android
gradlew assembleDebug
```

APK будет в: `android\app\build\outputs\apk\debug\app-debug.apk`

---

## 📱 После установки APK

1. **Запустите сервер на компьютере:**
   ```bash
   start-server.bat
   ```
   Запомните IP адрес (например: `http://192.168.1.100:5000`)

2. **Откройте приложение на телефоне**

3. **Настройте сервер:**
   - Меню (👤) → "⚙️ Настройки сервера"
   - Введите IP адрес: `http://192.168.1.100:5000`

4. **Готово!** Приложение подключится к серверу

---

## ❓ Проблемы?

**Ошибка "Android Gradle plugin requires Java 11":**
- У вас установлена Java 8, а нужна Java 11+
- **Решение:** См. файл `РЕШЕНИЕ_ПРОБЛЕМЫ_JAVA.md`
- **Или:** Используйте GitHub Actions (не требует установки Java!)

**Не могу собрать через GitHub:**
- Убедитесь, что код загружен на GitHub
- Проверьте, что файл `.github/workflows/build-apk.yml` существует

**Хочу собрать локально:**
- См. `BUILD_APK_SIMPLE.md` для подробной инструкции
- Или используйте `build-apk.bat` (если установлен Android SDK)
- **И нужна Java 11+** (см. `РЕШЕНИЕ_ПРОБЛЕМЫ_JAVA.md`)

**APK не устанавливается:**
- Включите "Установка из неизвестных источников" в настройках Android
- Или используйте ADB: `adb install app-debug.apk`

